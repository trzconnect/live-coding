/**
 *
 * Context of the problem:
 *
 * Given a string, find the length of the longest substring without repeating characters.
 *
 * input => 'abcdddefghi'
 *
 * output => 'defghi'
 */

describe("Find the longest non repeating char", () => {
  it("should return the longest non repeating char", () => {});
});
